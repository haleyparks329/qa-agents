# SaaS Dashboard Profile Rules

Use only simulated dashboard data. Prioritize role-based visibility, filter consistency, export behavior, empty states, and refresh behavior.

This profile is generic and public-safe. It must not include private workspace names, customer data, internal URLs, or real production metrics.
